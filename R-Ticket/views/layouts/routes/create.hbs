<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Добавить маршрут</title>
    {{> header}}
</head>
<body>
<div class="div">
    <form action="http://r-ticket.chav:6608/routes/create" method="POST">
        <label>Поезд
            <select size="1" name="trainId" style="width: auto">
                <option disabled selected>Выберите поезд</option>
                {{#trains as |train|}}
                    <option value="{{train.Id}}">{{train.Number}}: {{train.DepartureStation}} - {{train.ArrivalStation}}</option>
                {{/trains}}
            </select>
        </label>
        <br>
        <label>Начальная станция
            <select size="1" name="depStationId">
                <option disabled selected>Выберите начальную станцию</option>
                {{#stations as |station|}}
                    <option value="{{station.Id}}">{{station.Name}}</option>
                {{/stations}}
            </select>
        </label>
        <label>Конечная станция
            <select size="1" name="arrStationId">
                <option disabled selected>Выберите конечную станцию</option>
                {{#stations as |station|}}
                    <option value="{{station.Id}}">{{station.Name}}</option>
                {{/stations}}
            </select>
        </label>
        <br>
        <label>Время отправления
            <input type="time" name="depTime" size="50">
        </label>
        <label>Время прибытия
            <input type="time" name="arrTime" size="50">
        </label>
        <input type="submit">
    </form>
</div>
</body>
</html>