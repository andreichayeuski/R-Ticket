<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Новый пользователь</title>
    <script type="text/javascript" src="../jquery-3.3.1.min.js" ></script>
    <script type="text/javascript" src="../scripts/userCreate.js" ></script>
    <script type="text/javascript" src="../jquery.maskedinput.min.js"></script>
    {{> header}}
</head>
<body>
<div class="div">
    <div class="form">
        <form action="http://r-ticket.chav:6608/user/create" method="POST">
            <label>Цена
                <input required type="text" name="price" size="50">
            </label>
            <br>
            <label>Страна
                <select required size="1" name="spaceId">
                    {{#spaces as |space|}}
                        <option value="{{space.Id}}">{{space.Munber}}</option>
                    {{/spaces}}
                </select>
            </label>
            <br>
            <label>Роль
                <select required size="1" name="roleId">
                    {{#roles as |role|}}
                        <option value="{{role.Id}}">{{role.Name}}</option>
                    {{/roles}}
                </select>
            </label>
            <br>
            <label>Пол
                <input required type="radio" value="0" id="woman" name="sex">
                <label for="woman">Ж</label>
                <input type="radio" value="1" id="man" name="sex">
                <label for="man">М</label>
            </label>
            <br>
            <input type="submit">
            <input type="reset">
        </form>
    </div>
    <div>
        <h4>Пользователи</h4>
        <table>
            {{#users as |user|}}
                <tr>
                    <td>{{user.FName}} {{user.SName}}</td>
                </tr>
            {{/users}}
        </table>
    </div>
    <div class="link">
        <a href="http://r-ticket.chav:6608/user">Вернуться к списку пользователей</a>
    </div>
</div>
</body>
</html>